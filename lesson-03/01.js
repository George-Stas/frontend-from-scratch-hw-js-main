// Напишите функцию calculateAverage, которая принимает 2 числа и возвращает их среднее арифметическое

function calculateAverage(num_1, num_2) {
    const arifMean = (num_1 + num_2) / 2
    return arifMean 
}

const average = calculateAverage(5, 3)

console.log(average)
