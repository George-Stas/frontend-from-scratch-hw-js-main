// Разработайте функцию findLargest, которая принимает три числа и возвращает наибольшее из них.

function findLargest(num_1, num_2, num_3) {
    return Math.max(num_1, num_2, num_3)
}

const numMax = findLargest(5, 10, 3)

console.log(numMax)
